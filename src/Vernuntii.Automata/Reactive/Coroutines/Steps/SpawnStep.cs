namespace Vernuntii.Reactive.Coroutines.Steps;

// TODO:
/// <summary>
/// A 'spawned' process though will not block the parent, so the next 'yield' statement is called immediately.
/// Also the parent process will not be able to catch any exceptions that happen in the 'spawned' process.
/// </summary>
internal class SpawnStep
{
}
